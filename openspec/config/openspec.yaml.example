# openspec.yaml — example change configuration
#
# This file is created by `openspec new change <name>` inside openspec/changes/<name>/.
# It describes the change and the schema driving its artifact workflow.
#
# You do not edit this file manually — it is managed by the openspec CLI.
# It is included here as a reference for what a typical change config looks like.

name: add-dark-mode
schema: spec-driven
created: "2026-06-03T10:00:00Z"
status: in-progress

# Artifacts generated for this change (managed by openspec CLI):
#   proposal.md   — what & why
#   design.md     — how
#   specs/        — requirement deltas
#   tasks.md      — implementation checklist
